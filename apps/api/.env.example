# Backend API Environment Variables

NODE_ENV=development
PORT=3000
FRONTEND_URL=http://localhost:5174
TRUST_PROXY_HOPS=1
AUTO_RUN_MIGRATIONS=false

# API Keys (server-side only)
GEMINI_API_KEY=your_gemini_key_here
GOOGLE_MAPS_API_KEY=your_google_maps_key_here

# BNSF (optional, will use tariff fallback if not provided)
FEATURE_BNSF_RATES_ENABLED=false
# BNSF_CERT_PATH=/path/to/cert.pem
# BNSF_KEY_PATH=/path/to/key.pem

# Firecrawl (daily bid scraping — $19/mo starter plan)
FIRECRAWL_API_KEY=fc-your_key_here

# Redis (server-side cache — Upstash free tier or Railway add-on)
# REDIS_URL=redis://default:password@host:port

# Database (local Postgres in Docker on Mac mini)
DATABASE_URL=postgresql://cornintel:cornintel_local_change_me@localhost:5432/cornintel
